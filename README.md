# Oasis React SDK (POC)

This repository contains a minimal proof-of-concept React SDK for rendering LLM messages and a small example app that demonstrates usage.

Usage:

- Build the library: npm install && npm run build
- Run the example: npm run start:example

The SDK exposes GenUIProvider and MessageRenderer as the minimal API.
